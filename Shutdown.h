#ifndef SHUTDOWN_H
#define SHUTDOWN_H

#include "PowerPlantState.h"

/**
 * @class Shutdown
 * @brief Represents the shutdown state of a power plant.
 *
 * This class derives from PowerPlantState and signifies that the power
 * plant is in a shutdown state. Specific behavior and attributes related 
 * to this state can be implemented in this class.
 */
class Shutdown : public PowerPlantState {
    // Future implementation can include attributes and methods specific to the shutdown state
};

#endif // SHUTDOWN_H
